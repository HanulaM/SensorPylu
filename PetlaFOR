//wait 60s for sensor to warm up
  for (int i = 1; i <= 60; i++)
  {
    delay(1000); // 1s
    Serial.print(i);
    lcd.setCursor(0, 0);
    lcd.print(i);
    Serial.println(" s (wait 60s for sensor to warm up)");
  }
